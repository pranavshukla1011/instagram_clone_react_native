import React from 'react';

const initialState = {
  sample: null,
};
const sampleReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default sampleReducer;
